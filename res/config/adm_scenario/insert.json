{
    "validation": {
        "title": {
            "title"   : "제목",
            "required": true,
            "filter"  : "chkNull"
        }
    },

    "authority" : "{__FUNC__.chk_authority}({_DEFINE._AUTH_SUPERADMIN_LEVEL})",

    "model": {
    },

    "data":{
        "insert" : {
            "query"  : null,
            "query_type": "insert",
            "scheme" : {
                "title" : {
                    "value" : "{__REQ__.title}"
                }
            },
            "params" : {
                "table"  : "{__TABLES__.chat_scenario}",
                "where"  : null
            }
        }
    },

    "alarm" : null,

    "output": {
        "result": "true",
        "msg": "{__SYSMSG__.v_insert}"
    }
}